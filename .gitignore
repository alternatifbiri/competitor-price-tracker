tracker.db
logs/
exports/
reports/
data/
__pycache__/

# Google service account keys, never commit these
credentials.json
credentials/
service-account*.json
*-service-account.json
*.credentials.json

# config.json is committed on purpose, the Dockerfile copies it at build
# time. Keep smtp.password, telegram.bot_token and the service account path
# empty in here and supply the real values through the environment.
